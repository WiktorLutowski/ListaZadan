<Window x:Class="Lista.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lista.Views"
        xmlns:vm="clr-namespace:Lista.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="400">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBox Padding="3" Height="auto" Name="textBox" Text="{Binding NewElementContent}" TextChanged="textBox_TextChanged" VerticalContentAlignment="Center" FontSize="20"/>
        <Label Margin="3 0 0 0" VerticalAlignment="Center" Foreground="#AA000080" Name="placeHolderLabel" MouseDown="placeHolderLabel_MouseDown" FontSize="20" Content="Nowy element" Height="auto"/>

        <Button Content="Dodaj" Background="#DC143C" Foreground="White" Grid.Column="1" FontSize="20" Padding="10" Command="{Binding AddCommand}"/>

        <ListView Grid.Row="1" Grid.ColumnSpan="2" ItemsSource="{Binding Elements}" HorizontalContentAlignment="Stretch">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding}" FontSize="20"/>
                        <Separator Background="Crimson"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
